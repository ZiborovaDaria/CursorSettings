# Sync No WebFetch / cherry-pick pack into C:\Cursor\* projects

Source of truth after update: this repo (CursorSettings).  
Run from repo root after pull:

```powershell
.\tools\sync-cursor-projects.ps1
```

Does not rewrite project `mcp.json` or `.dev.env`.
