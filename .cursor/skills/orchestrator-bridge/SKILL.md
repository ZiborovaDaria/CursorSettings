# Orchestrator Bridge Skill

Use when invoked by `orchestrator.py`, `cursor agent`, `AI_AGENT`, or when prompt asks for `tool_plan_json`.

Return JSON only. No Markdown.

Load rule: `project-esti-orchestrator-bridge-agent.mdc`.
