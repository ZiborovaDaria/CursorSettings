---
description: Mandatory compact Hub consult before generating 1C code. Gate → Lookup ≤2 → Proof KB.
alwaysApply: true
---

<!-- DO NOT EDIT — synced from C:\1c-shared-patterns\cursor-addons\rules-shared\hub-gate.mdc -->

# Hub Gate (mandatory, compact)

Перед генерацией или существенной правкой BSL, CFE/EPF/ERF, управляемых форм, Excel, query:

1. Прочитай `C:\1c-shared-patterns\playbooks\agent-lessons\index.md`.
2. При CFE/EPF/ERF/формах/печати/Excel — skill `reuse-1c-shared-patterns` (content-patterns index).
3. Открой **максимум 2** совпавших lesson/pattern файла.
4. Пиши код только после шагов 1–3.
5. В ответе одна строка: `KB: <id1>, <id2>` | `KB: none` | `KB: skip-cosmetic`.

Не читай весь Hub.  
Исключение: чисто косметический rename/комментарий без новой логики → `KB: skip-cosmetic`.  
После check/runtime fail — снова consult lessons + skill `error-learning-1c` (promote в Hub, не только local reflection).

Skills: `consult-1c-shared-lessons`, `reuse-1c-shared-patterns`.
