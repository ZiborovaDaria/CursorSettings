# 1C Testing Release Skill

Use before final delivery.

1. Run strongest available check.
2. Do not fake verification.
3. Use `deploy_and_test` for runtime/CFE/metadata changes.
4. Capture failed checks with `capture-error`.

Load rule: `1c-testing-release-agent.mdc`.
