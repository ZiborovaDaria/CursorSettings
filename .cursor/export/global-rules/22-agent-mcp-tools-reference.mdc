---
description: USE WHEN choosing MCP tool for 1C — общий справочник (без привязки к конфигурации).
alwaysApply: false
---

# Справочник MCP-инструментов 1С (общий)

Контекст конфигурации и доступность ИБ — в workspace: `03-agent-configuration-and-mcp-selection.mdc`, `MCP_ROUTER_*.md`, `mcp.json`.

**ЭСТИ:** полная матрица ~114 tools → `.cursor/MCP_TOOLS_MATRIX.md` (не дублировать здесь).

## litecode
- `search_by_embedding` — неизвестно точное имя.
- `search_metadata` — `browse`, `object_structure`, `get_form`, `get_routines`, `get_routine_body`, `get_call_graph`.

## code-index
- `search_function`, `get_function`, `find_symbol`, `grep_code`, `get_callers`, `get_callees`.

## serena
- `find_symbol`, `get_symbols_overview`, `find_referencing_symbols`
- `replace_symbol_body`, `replace_content`

## codegraph
- `codegraph_explore`, `codegraph_impact`, `codegraph_callers/callees`

## lean-ctx
- `ctx_read`, `ctx_search`, `ctx_tree`, `ctx_shell` — см. `23-agent-code-intelligence-router.mdc`

## 1c-naparnik, 1c-syntax-helper, v8std
- Проверка BSL, ITS, платформа, стандарты.

## С ИБ (если включено в проекте)
`mcp-1c`, `1c-rest-mcp` — шаблоны в `mcp.optional.json` workspace.
