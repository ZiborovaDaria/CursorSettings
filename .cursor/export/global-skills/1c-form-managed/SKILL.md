# 1C Managed Form Skill

Use for managed form modules and form XML.

1. Client code is UI only.
2. Server logic goes to server procedure/common module.
3. Do not perform queries from client.
4. Avoid reserved names and event signature guesses.
5. Locate existing form events before editing.

Load rule: `1c-managed-forms-agent.mdc`.
